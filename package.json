{
  "name": "slclean",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "verify:ui": "node scripts/verify-ui-in-headless-chrome.mjs",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "playwright-core": "^1.62.1"
  },
  "description": "Clean caches, temp folders and build artifacts on Windows (Tauri 2 + Rust)",
  "license": "MIT",
  "repository": "github:SalyyS1/SLClean"
}
